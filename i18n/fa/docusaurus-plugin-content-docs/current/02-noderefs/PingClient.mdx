---
sidebar_position: 349
title: "PingClient"
sidebar_label: "PingClient"
---

<!-- Documentation version: 154 -->

# PingClient

> اطلاعیهٔ نسخهٔ ۲: این صفحه جایگزین مستندات قدیمی Ping wire v1 است. گزینه‌ها و
> راهبردهای قدیمی مانند `strategy`، `wrap-*`/`warp-*`، `source`، `dest`،
> `check-identifier`، `xor-byte` و `roundup-size` دیگر معتبر نیستند و برنامه آن‌ها
> را با خطای مهاجرت رد می‌کند.

`PingClient` در نسخهٔ ۲ یک بستهٔ کامل IPv4 را در یک ICMP Echo Request واقعی
حمل می‌کند. همتای آن، `PingServer`، Echo Reply با نوع `0` و با شناسه، شمارهٔ
ترتیب و دادهٔ دقیق همان درخواست می‌فرستد. Reply فقط تأیید دریافت است و دادهٔ
جدید جهت مخالف همیشه در یک Echo Request جدید حمل می‌شود.

رهگیری درخواست‌های در انتظار و جلوگیری از replay در یک رهگیر همگام‌شده برای
کل node انجام می‌شود و به WID خط بسته وابسته نیست؛ هر حلقه حداکثر ۱۰۲۴ ورودی
دارد.

تنظیمات معتبر فقط این‌ها هستند:

| گزینه | الزامی | پیش‌فرض |
| --- | --- | --- |
| `local-ipv4` | بله | — |
| `peer-ipv4` | بله | — |
| `identifier` | خیر | `"random"` |
| `sequence-start` | خیر | `1` |
| `ttl` | خیر | `64` |
| `tos` | خیر | `0` |

حداکثر بستهٔ داخلی ۱۴۷۲ بایت است و سربار حامل ۲۸ بایت است. IPv4 داخلیِ
fragmented معتبر است، ولی حامل خارجی fragmented، IPv6، بستهٔ malformed یا
checksum نامعتبر رد می‌شود. برای توضیح کامل و مثال پیکربندی فعلی، به
[مرجع انگلیسی PingClient](https://github.com/radkesvat/WaterWall-Docs/blob/main/docs/02-noderefs/PingClient.mdx)
مراجعه کنید.
